// The configure script and makefile in the binary distribution try to
// run this program to see if the platform is right. We ASSume that people
// do not put together binary distributions where the different components
// are compiled for different architectures.
int main() {
    return 0 ;
}
